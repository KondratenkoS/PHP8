<?php
	/*
		Не запуская код, определите, что выведется на экран:
	*/
	$test = 'null';
	
	if (!empty($test)) {
		echo '+';
	} else {
		echo '-';
	}
	
	//'+' 'null' это строка, значит переменная не пуста